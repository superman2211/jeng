export default abstract class BitmapFilter {
	// eslint-disable-next-line class-methods-use-this, @typescript-eslint/no-unused-vars
	apply(width: number, height: number, sourcePixels: number[], targetPixels: number[]) {
	}
}
