export enum BitmapDataChannel {
	RED = 1,
	GREEN = 2,
	BLUE = 4,
	ALPHA = 8,
}

export enum BitmapDataCompare {
	IDENTICAL = 0,
	WIDTH_NOT_EQUAL = -3,
	HEIGHT_NOT_EQUAL = -4,
}

export enum PixelSnapping {
	AUTO = 'auto',
	ALWAYS = 'always',
	NEVER = 'never',
}
